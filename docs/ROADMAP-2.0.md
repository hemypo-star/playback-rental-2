# Playback Rental 2.0 — Consolidated Roadmap

*Compiled 2026-08-20 from every work-plan document found in the project, cross-checked against the actual code and git history — not just the docs' own claims.*

Three plans exist, written in sequence, each building on the last:

| # | Document | Scope |
|---|---|---|
| 1 | `~/.claude/plans/rosy-booping-spindle.md` *(local file, outside the repo)* | The master plan — audit + rewrite the whole site as Astro + Payload |
| 2 | [docs/PLAN-docker-admin.md](PLAN-docker-admin.md) | Replace Payload's stock admin with a custom UI, move everything into Docker |
| 3 | [docs/PLAN-next-migration.md](PLAN-next-migration.md) | Fold the Astro app into Payload's own Next.js app — kills the two-process split plan #2 introduced |

Below: each plan's own checklist, ~~struck through~~ where the code confirms it's actually done, followed by the open items every plan leaves behind, the places the three plans disagree with each other or with the code, and a suggested order of attack.

---

## 1. Master plan — `rosy-booping-spindle.md`

The original scope: audit the old Supabase/Vite app, don't rebuild its dead code, replace it with Astro + Payload + a МойСклад sync, cut over, retire the old stack.

- ~~**Phase 0 — Setup & МойСклад spike**~~ — done 2026-08-12. Branch `2.0`, monorepo scaffolded, account structure mapped (rental listings are Услуга/service entities, matched to a parallel Товар/product tree for stock).
- ~~**Phase 1 — Payload backend + МойСклад sync**~~ — done 2026-08-12. Collections, price/availability hook, two-way sync, order push to МойСклад, notification webhook.
- ~~**Phase 2 — Astro storefront**~~ — done 2026-08-13/14, plus the custom admin UI and Docker deployment that weren't in the original Phase 2 scope at all (see plan #2 below — added mid-stream, not planned here).
- **Phase 3 — Cutover** — **not started.** `main` still deploys the legacy Vite/Supabase app on every push (`.github/workflows/*.yml`, `pm2 reload ecosystem.config.cjs`); the `2.0` branch has never been merged or pointed at a domain.
- **Phase 4 — Retire old stack** — **not started**, blocked on Phase 3.

**Reality check on the estimate:** this plan budgeted "~5.5–7.5 weeks" total for Phases 0–4. Phases 0–2 alone have run 2026-08-12 → 2026-08-20 and, per the dev log, absorbed an entire second project (custom admin + Docker, plan #2) that this plan never scoped, and are about to absorb a *third* (the Next.js merge, plan #3) before Phase 3 can start. Nobody has re-cut the estimate since — worth doing before promising a cutover date.

---

## 2. `docs/PLAN-docker-admin.md`

Two goals: a fully custom `/admin` UI (Payload demoted to headless backend, keeping `/cms` as a safety net), and everything running through Docker on one public port.

| Step | Scope | Status |
|---|---|---|
| 0 | Design reference extraction, plan committed to repo | ~~Done~~ |
| 1 | Proxy fixes (gzip double-decode bug, Server Actions origin, split `CMS_INTERNAL_URL`/`PUBLIC_PAYLOAD_URL`, Payload admin moved to `/cms`) | ~~Done~~ — verified live 2026-08-14 |
| 2 | Docker (`compose.yaml`/`compose.dev.yaml`, multi-stage Dockerfiles, single published port) | ~~Done~~ — verified with a real `docker compose up` |
| 3 | Admin shell + auth (`/admin/*` guard, JWT-header SSR auth, login/logout) | ~~Done~~ |
| 4 | Dashboards (KPI, orders, calendar, stock, clients, analytics — 6 new `GET /api/admin/*` endpoints) | ~~Done~~ — verified against real synced data |
| 5 | Order operations (status, notes, item edits, delete, manual МойСклад submit) | ~~Done~~ — verified, including the `endDate < startDate` hook-error path |
| 6 | Catalog editing (Categories/Promotions CRUD, Products edit-only, image upload) + the deferred promotion detail page (`/promotions/:slug`) | ~~Done~~ |
| 7 | Long tail (`/admin/settings`, `/admin/media`, `/admin/users`) | ~~Done~~ |
| 8 | Retire `/cms` from the proxy allow-list (keep the route as a break-glass fallback) | **Not done — intentionally.** Plan requires "a week of the owner actually using `/admin`" first; it's been ~6 days since Step 7 shipped (2026-08-14 → today). Also now entangled with plan #3, see [Conflict 2](#conflicts) below. |

**Bonus item this plan doesn't track:** the long-standing "unstyled Payload admin" bug called out as unresolved in `CLAUDE.md`'s design-system section — root-caused and fixed during Step 1 (missing `@payloadcms/next/css` import). ~~Resolved.~~

---

## 3. `docs/PLAN-next-migration.md`

Explicitly "a continuation of PLAN-docker-admin.md" — folds `apps/web` (Astro) into `apps/cms` (Next.js) entirely, because Payload's Local API only works in-process, and the two-process split from plan #2 is what forces the proxy/REST/dual-URL machinery that plan describes. End state: one Next.js app, `apps/web` deleted.

This is the **newest** plan (committed today, 2026-08-20, alongside the first real code from it). Stage 0 (prep) and Stage 1 (shell + proxy reversal) landed first; Stage 2 (storefront port) is now done as well, all in the same day — Stages 3–4 remain:

**Stage 0 — Prep**

- [x] 0.1 Scope `typescript.ignoreBuildErrors`/`eslint.ignoreDuringBuilds` to specific paths instead of the whole app — **done 2026-08-20.** Added `apps/cms/eslint.config.mjs`; `no-explicit-any` suppression scoped to `app/(payload)/**`, `endpoints/**`, `components/admin/**`, `lib/rental/**`; `eslint.ignoreDuringBuilds` removed entirely. The handful of errors this surfaced outside those paths (real `any`s in `collections/OrderItems.ts`/`Orders.ts`, three admin nav links using `<a>` instead of `next/link`) were fixed, not suppressed.
- [x] 0.2 Upgrade Next 15 → 16.3 before migrating — **done 2026-08-20**, `apps/cms/package.json` now pins `"next": "^16.3"` (16.3.1 resolved). The `ReactPortal`/`LayoutProps` bug isn't fixed by the upgrade alone, but combined with the existing Fragment-wrap workaround in `layout.tsx`, `next build`'s generated-type check now passes — `typescript.ignoreBuildErrors` was removed.
- [x] 0.3 Extract the design bundle into a machine-checkable spec — `tools/design-sync.mjs` and `docs/design-reference/spec/{tokens.css,interactions.css}` exist, and both files are now actually wired in (`apps/cms/src/styles/global.css` imports both, done as part of Stage 1 — see below), not just generated.
- [ ] 0.4 Freeze the manual smoke-test checklist — still not done as a formal written checklist. Stage 1 itself was verified live (real `next dev` + `astro dev` against a real Postgres, not just `next build`), but that was ad hoc, not against a frozen list — worth doing before Stage 2 starts touching real pages.
- [ ] 0.5 Leave `MOYSKLAD_API_TOKEN`/`NOTIFICATION_WEBHOOK_URL` alone — trivially true so far (nothing's touched them).

**Stage 1 — Shell + proxy reversal** — **done 2026-08-20**, verified live. `apps/cms` (Next) is now the single public entry point; `apps/cms/src/proxy.ts` (named for Next 16's renamed convention, not `middleware.ts`) fallback-proxies everything not yet ported to the still-live Astro app, which lost its own proxy logic entirely (kept only the `/admin` session guard). `apps/cms/src/app/(frontend)/layout.tsx` exists as a skeleton (no Navbar/Footer yet — Stage 2 scope). One real bug found only by running real `next dev` + `astro dev` concurrently, not by `next build` alone: a same-origin redirect with a relative Location header crashed Next's Node-runtime proxy handling (`TypeError: Invalid URL`) the way Astro's own proxy never did — fixed by always rewriting to an absolute URL. `(admin)` route group still doesn't exist — that's Stage 3.

**Stage 2 — Storefront port** (7 page groups, Local API data layer, cache-freshness handling) — **done 2026-08-20**, all 7 page groups landed the same day they were started. Page group 1 of 7 done (legal pages: `/privacy-policy`, `/user-agreement`, both dropped out of `proxy.ts`'s fallback matcher). Since every page needs it, this pass also built the shared chrome Stage 1 deliberately deferred — Navbar/Footer, the `lib/data/siteSettings.ts` Local API layer, and near-verbatim ports of CartBadge/AdminPanelLink/RentalDatePicker — so page groups 2–7 inherit working chrome instead of each needing to build it. `design-sync audit` run against the ported chrome and fixed to spec (20/21 transitions now have explicit `ease-*`/`duration-240`, one pre-existing gap left alone in shared `global.css` to avoid apps/web drift). Page group 2 of 7 done (`/how-it-works` static port, `/contact` via `getSiteSettings()`, `ContactForm` client island posting to `apps/cms`'s own `/api/contact-notification` endpoint directly instead of `apps/web`'s REST wrapper — no new backend code, that endpoint already existed). Page group 3 of 7 done 2026-08-20: the homepage (`app/(frontend)/page.tsx`, 297-line source, the largest template in the plan), `export const dynamic = 'force-dynamic'` since stock/availability/promotions aren't visible to Next at request time; new `lib/data/{categories,products,promotions}.ts` and `lib/mediaUrl.ts`/`lib/categoryTree.ts` (the latter's `buildCategoryTree`/`flattenCategoryTree` carried over unused, needed by the catalog sidebar next); new `ProductCard.tsx` (plain Server Component, relies on part 1's delegated click listener) and `PromoCarousel.tsx` (client island, ported verbatim); `proxy.ts` matcher fixed to actually exclude the now-ported bare root `/` (previously fell through to Astro regardless, since an empty remainder trivially satisfies a negative lookahead over non-empty alternatives). Verified live against real `next build`/`next dev`+`astro dev`, including confirming category-tile/kit/marquee sections' absence in the empty local dev DB was genuinely zero-data, not a rendering bug. **Page group 4 of 7 done 2026-08-20**: catalog (`catalog/page.tsx` + `catalog/[slug]/page.tsx`, both `force-dynamic`; `[slug]` redirects to `/catalog` on an unresolved slug and has its own `generateMetadata()`, backed by a new `getCategoryBySlug()` wrapped in React's `cache()` since Local API calls don't get `fetch()`'s automatic per-request dedup); `CatalogPage.tsx` (plain Server Component) and `CategorySidebar.tsx` (new `'use client'` — the mobile `<select>`'s `onChange` needs a real handler, unlike Astro's string `onchange="..."`); `catalog-availability.ts` duplicated from `apps/web` (same duplicate-not-move pattern as `cart-actions.ts`), wired via `CatalogAvailabilityInit.tsx` (same `useEffect`-wrapper pattern as part 1's `CartActionsInit.tsx`); new `lib/rentalAvailability.ts`, a browser-only client hitting this app's own same-origin `/api/rental-availability-bulk` directly (the old REST client's `CMS_INTERNAL_URL`/`PUBLIC_PAYLOAD_URL` split doesn't apply here). `proxy.ts` matcher extended to exclude `catalog`. Verified live with a real (temporary, deleted before commit) seeded category + 2 products, since the dev DB otherwise has no synced МойСклад data: sidebar category count, both product cards, `/catalog/[slug]`, `?q=` search filtering, and the redirect-on-unknown-slug all curl-confirmed; `/product/1` still correctly falls through the proxy to Astro. **Page group 5 of 7 done 2026-08-20**: product detail (`product/[id]/page.tsx`, `force-dynamic`, both `generateMetadata()` and the page component funneled through one shared `loadProduct()` helper rather than React's `cache()` — a plain shared async function was enough here since, unlike part 4's `getCategoryBySlug()`, there's no risk of the same lookup running twice across different call sites); `getProductById()` (`lib/data/products.ts`) now returns `Product | null` via Payload's `disableErrors: true` on `findByID`, replacing the REST client's throw-and-instanceof-check pattern the Astro source used; new `getRentalAvailability()` (singular) in `lib/rentalAvailability.ts` alongside part 4's bulk version, same browser-only/sessionStorage reasoning; two new islands, `ProductPurchasePanel.tsx` and `QuantitySelector.tsx` (verbatim). `ProductPurchasePanel.tsx` hit a new `react-hooks/set-state-in-effect` case beyond the already-familiar `mounted` guard: a synchronous `setChecking(true)` before an availability fetch starts, suppressed inline with its own rationale since the rule's own recommended shape doesn't cleanly cover an initial-loading flag. `proxy.ts` matcher extended to exclude `product`. Verified live with a real (temporary, deleted before commit) seeded product + category: title/subtitle/description/breadcrumb/category-badge/in-stock-badge/related-products all curl-confirmed, including price formatting specifically (`Intl.NumberFormat`'s non-breaking space, caught byte-level after a naive grep missed it) and that `ProductPurchasePanel`'s props are present in the RSC payload for hydration; both a non-numeric and an out-of-range numeric id correctly 307-redirect to `/catalog`; `/checkout` still correctly falls through the proxy to Astro. **Page group 6 of 7 done 2026-08-20**: promotions detail (`promotions/[slug]/page.tsx`) — the smallest port so far, since the `Promotions` collection, its admin CRUD, and this route's content model (slug/kicker/text/content/linkedProducts/linkedCategories) already existed from 2026-08-14; no new data-layer code needed, `getActivePromotions()`/`getPromotionBySlug()` were both already written in part 3 and just sat unused until now. `Astro.redirect('/404')` became `next/navigation`'s `notFound()` (apps/web never had a real `/404` route either, so the original just fell through to Astro's default not-found handling). `proxy.ts` matcher extended to exclude `promotions`. Verified live with a real seeded promotion (needed more state than any prior part — `Promotions.image` is a required upload field, and both the linked-products and linked-categories sections needed real linked data), including confirming the uploaded media file was actually removed from disk (not just its DB row) during cleanup: title/kicker/content/image, the linked-products section (a real `ProductCard` with its `/api/media/file/...` URL), and the linked-categories tile all curl-confirmed; a nonexistent slug correctly 404s via `notFound()`. **One discrepancy caught reviewing the commit, fixed before pushing**: the commit message initially claimed `getPromotionBySlug()` "gained the same React `cache()` wrap part 4 gave `getCategoryBySlug()`" — the first-pass diff only added `import { cache } from 'react'` to `lib/data/promotions.ts` without wrapping the function itself, so the claim didn't match the code (`eslint`/`tsc --noEmit` don't flag an unused-but-imported `cache`, so it slipped past the checks actually run). Caught while reviewing the roadmap-chronicler's independent verification of the diff, and fixed in the same unpushed commit rather than left as a follow-up — `getPromotionBySlug` is now genuinely `cache(async (slug) => ...)`. `/checkout` (last page group) still falls through the proxy to Astro — **checkout is next up**, and per the plan is the one page group with a real mutation (`submitOrder()` → Server Action on the Local API, replacing the browser-side REST POST and retiring the `mutate()`/`{ doc, message }` wrapper along with it), so it needs more care than parts 1–6.

**Page group 7 of 7 done 2026-08-20 — Stage 2 complete.** Checkout (`checkout/page.tsx` + `components/CheckoutPage.tsx`), the plan's own flagged "needs the most care" page group, since it's the only one with a real mutation — and the only page group that got a genuine architectural change rather than a syntax port. `submitOrder()` becomes a Server Action (`checkout/actions.ts`'s `submitCheckout()`) using the Local API directly, replacing the old REST flow's 1+N+1 separate HTTP round trips (`createOrder`, one `createOrderItem` per cart line, `submitOrder`) through `apps/web`'s `mutate()`/`{ doc, message }` wrapper. The `/:id/submit` endpoint's business logic (МойСклад push, notification webhook, `submittedAt` stamping) — previously inline in its handler (`collections/Orders.ts`) — was extracted into `lib/rental/submitOrder.ts` so the new Server Action and the still-needed endpoint (kept as-is for `apps/web`'s REST-based checkout until Stage 4) call identical code instead of duplicating it; the endpoint is now a thin wrapper that still owns the `submitToken` check specifically, since that's an HTTP-boundary-specific authorization concept the Server Action doesn't need (it only ever submits the order it just created in the same request). The Server Action's `orders`/`orderItems` creates explicitly pass `overrideAccess: false` (Local API defaults to `true`, i.e. bypassing access control) since this is genuinely public anonymous checkout and should be evaluated under the same collection access rules a real anonymous REST caller hits — not silently exempted from them. Kept the original's rollback-on-item-failure behavior (delete any orderItems already created if a later cart line fails validation) and its "leave the order shell in place" behavior (deleting orders is admin-only by design). Two Payload-specific TypeScript gotchas hit wiring the Local API `create()` calls: an explicit `notes: undefined` key routed `payload.create()`'s overload resolution into a confusing "missing draft property" error (fixed by only including the key when actually set), and `status` still had to be passed explicitly despite having a schema `defaultValue`, since Payload's generated `Data` type for a `required` field doesn't know about runtime-applied defaults. `proxy.ts` matcher extended to exclude `checkout` — nothing left in the fallback matcher's exclusion list is unported. Verified live more thoroughly than any prior part, using a real Playwright/Chromium browser session (confirmed available globally on this machine, not a project dependency) for the first time in this migration: picked dates on a real seeded product, added to cart, filled and submitted the checkout form, reached the real "Заявка отправлена!" success state with zero console errors; a direct Local API query afterward confirmed the order/orderItem actually persisted (correct `totalPrice`/`lineTotal`/dates/status, `submittedAt` genuinely set) and that the graceful-МойСклад-failure path was really exercised, not just theoretically present — the dev server log showed the real caught "MOYSKLAD_API_TOKEN is not set" error (no live credentials touched, per this project's standing rule) and checkout still completed. A second, adversarial browser run pushed the requested quantity past seeded stock (6 vs 3 available) — the `beforeValidate` hook's exact validation message surfaced in the UI's error banner, and the DB afterward showed the expected partial-failure state (an orphaned order shell with zero items, no orphaned orderItem), confirming the rollback design end to end. All seeded test data and scratch scripts deleted before committing. `next build` compiles `/checkout` as static (unlike the other 6 page groups — no server data fetching of its own, the cart is entirely client-side localStorage). Lint clean under the same `react-hooks/set-state-in-effect` suppression pattern established in parts 1 and 5. `design-sync audit` shows no new gaps. **This closes Stage 2 — all 7 page groups done.**

**Stage 3 — Admin port** (auth simplification, 7 endpoint→server-function conversions, Server Actions for mutations) — **started 2026-08-20**, first commit (`614dcb6`) landed the same day Stage 2 finished. Covers plan sections 3.1 (auth) + 3.2 (shell) + page group 1 of 6 per section 3.5 (`login`, `first-register`). `apps/cms/src/lib/admin/auth.ts` replaces `apps/web/src/lib/admin/session.ts` (no more cross-process `Authorization: JWT` re-forward — `payload.auth({ headers: await headers() })` reads cookies in-process); a new `(admin)/admin/layout.tsx` route group hosts the real auth guard (`AdminSidebar.tsx`, `AdminPageHeader.tsx` are its new shell pieces); `/admin/login`/`/admin/first-register` live under `(frontend)`, outside the guard, matching the Astro source's own layout choice for those two pages. One flagged simplification: the new guard's redirect to `/admin/login` doesn't preserve a `next=` deep-link param the way Astro's did — confirmed low-impact today since bare `/admin` (`apps/web/src/pages/admin/index.astro`) is genuinely nothing but a redirect to `/admin/orders`, not yet a real content page. Verified live with a real Playwright session: full register → guard → `/admin` → `/admin/orders` (still Astro, via proxy fallback) redirect chain, confirming session continuity survives the process boundary — the load-bearing thing to check about this specific commit.

**Second commit (`836aa91`)**, same day. Covers plan section 3.3 (`lib/admin/data/{kpi,orders}.ts` — the `endpoints/admin/{kpi,orders,orderDetail}.ts` endpoint bodies turned into plain server-only functions, minus the `req.user` check each endpoint needed for itself; those endpoints are untouched, still serving `apps/web`'s REST-based admin until Stage 4), 3.4 (Server Actions for mutations), and page group 3 of 6 per 3.5 (`orders` list + `orders/[id]`, the plan's own flagged most-complex page group — status/notes/positions/delete/submit-to-МойСклад). New `(admin)/admin/orders/[id]/actions.ts` Server Actions each re-check `getAdminUser()` themselves before mutating, beyond what 3.3 covers for reads: a Server Action compiles to its own independently-invokable endpoint and is *not* gated by the layout guard just because the referencing page lives under it (documented Next.js behavior), and a Local API call has no `req.user` of its own to satisfy Orders'/OrderItems' access control unless a request context is built and authenticated by hand — skipping the check wouldn't fail safe, it would fail broken. `submitOrderToMoySklad` calls the same `lib/rental/submitOrder.ts` the checkout Server Action and the REST `/:id/submit` endpoint already share — third caller, still one implementation.

**Real bug found and fixed verifying `submitOrderToMoySklad` live**: it failed every time with "Unauthorized," under the same valid, unexpired admin session every other mutation on the same page succeeded under. Root cause: `payload.config.ts`'s `cors`/`csrf` allowlist is hardcoded to `WEB_URL`, and `apps/cms/.env.example` (the local, non-Docker dev copy — root `.env.example`/`compose.yaml` were already correct) still set it to `apps/web`'s own old address (`http://localhost:4322`), stale since Stage 1 made `apps/cms` the single public entry point. Payload's cookie-JWT strategy (`extractJWT`) checks the request's `Origin` header against that allowlist when present, falling back to `Sec-Fetch-Site` (satisfied by same-origin requests regardless of the allowlist) only when Origin is absent. A plain page navigation typically carries no Origin at all, so every Server Component auth check kept succeeding — but a Server Action's own internal POST does send Origin, so every session-reading Server Action was silently rejected. This is also why Stage 1/2 never surfaced it: their only prior Server Action (checkout's `submitCheckout`) is anonymous and never calls `payload.auth()`. **Fixed** by correcting `WEB_URL` to `http://localhost:3000` (this app's own local address) in `apps/cms/.env.example`, with that file's comment and `payload.config.ts`'s neighboring `cors`/`csrf` comment both rewritten to state `WEB_URL` has meant this app's own public origin since Stage 1, not `apps/web`'s. Root-caused via a bare 10x `payload.findByID()` loop outside Next (10/10 succeeded, ruling out a DB/Payload-level bug), a temporary header dump inside `getAdminUser()` (cookie present and byte-identical on every call, ruling out a missing-cookie theory), and reproducing the identical failure under a genuine `next build && next start` production server as well as `next dev` (ruling out a dev-mode/Turbopack artifact) before reading Payload's own `extractJWT` source. Verified live with real Playwright sessions: login → orders list (real KPI cards + row) → order detail → status/notes/item-quantity edits (`lineTotal`/`totalPrice` recompute confirmed via a direct Local API query, not just the UI) → submit-to-МойСклад (same graceful-failure path Stage 2's checkout already verified) → item delete → order delete (confirmed gone via Payload's own `NotFound`, not just absent from a list).

**Third commit (`464c8f7`)**, same day. Page group 4 of 6 per 3.5 — `calendar`, `stock`, `clients`, `analytics`. All four read-only, so no new Server Actions this time — just four `lib/admin/data/*.ts` functions (same endpoint-body-minus-`req.user` pattern as `kpi.ts`/`orders.ts`) and four pages calling them directly; the four endpoints stay untouched. The calendar page's Gantt-bar hover already used the design's overshoot curve (`cubic-bezier(0.34,1.56,0.64,1)`, `scaleY(1.16)`) verbatim from the Astro source — the one spot section 3.6 flagged as still-linear turned out already correct, nothing to fix there. `proxy.ts` matcher extended to exclude all four routes. One tooling snag, not a shipped-code bug: the usual scratch Local API seed script (`tsx`) crashed on a `payload/dist/bin/loadEnv.js` destructure (`@next/env`'s `loadEnvConfig` came back `undefined` under `tsx`'s CJS transform, likely version skew — both `@next/env@16.3.1` and a stale `15.5.23` are present in `node_modules`) — worked around by seeding through direct authenticated REST calls instead, flagged as a follow-up rather than chased down. Verified live with a real Playwright session: login → `/admin/orders` (confirming the bare `/admin` redirect still works) → all four new pages, each showing real seeded data (Gantt bar, stock row, client card, revenue bar), zero console errors. `design-sync audit` clean — `bnRule` and `bnBar` both hit their exact design counts for the first time this migration.

**Fourth commit (`b3da8e1`)**, same day. Page group 5 of 6 per 3.5 — `categories`, `promotions`, `products/[id]`. Categories and promotions get full list + `[id]` detail (`id === 'new'` handles both create and edit from one route); `products` stays edit-only (`moySkladId` required+readOnly). Unlike page group 4, this group's mutations are genuine writes — each collection gets its own `[id]/actions.ts` Server Action file, same `requireAdmin()`-before-`overrideAccess` pattern page group 3's `orders/[id]/actions.ts` established. New `lib/admin/data/{categories,promotions,products}.ts` are deliberately separate from the storefront's own `lib/data/*.ts` (different depth/filter needs — admin wants the full set including hidden rows, storefront wants only active/available), except the product edit page, which reuses `lib/data/products.ts`'s existing `getProductById()` unchanged. `lib/admin/mediaUpload.ts` replaces `apps/web`'s Astro-specific `define:vars` + runtime-import upload script with a plain client module. `CategoryForm`/`PromotionForm`/`ProductForm.tsx` replace the Astro sources' raw DOM manipulation (image reorder/remove, kit-item rows) with ordinary React state arrays. `proxy.ts` matcher extended to exclude all three new admin routes. Verified live with a real Playwright session against real seeded data: category hierarchy/create/edit-parent-picker/delete all confirmed; a promotion created with a real uploaded image and linked product+category, its public `/promotions/:slug` page confirmed rendering the title and linked product (the first time this migration verified an admin write via its effect on the *public* site, not just the admin UI or an API query), then deleted with both its media doc and on-disk file confirmed actually gone (Payload doesn't cascade-delete referenced media, so this needed its own explicit check); a product's price/isKit/kitItems edit confirmed persisted via a direct API query. Zero console errors. `design-sync audit` clean, no new gaps.

One page group remains: `media`/`users`/`settings` — the last of Stage 3.

**Stage 4 — Cleanup** (delete `apps/web`, drop the proxy/dual-URL/cors machinery plan #2 built) — not started, and not startable until Stage 3 is done.

**Net:** of the ~7 estimated working days in this plan, Stage 0 (0.1/0.2/0.3), Stage 1, and Stage 2 (all 7 page groups) are done as of 2026-08-20; Stage 3 is now underway (5 of 6 page groups landed across four commits the same day); Stage 4 is still fully unstarted and blocked on Stage 3.

---

## Unplanned work found in the code, not in any plan

**Hierarchical categories.** Today's commit (`a4c958f`) also added a `parent` relationship field to the `Categories` collection and a new `apps/web/src/lib/categoryTree.ts` (tree-building + depth-first flatten, used by `CatalogPage.astro`/`CategorySidebar.astro`). This is real, working scope — but it appears in **none** of the three plans, and the delivered design reference (`docs/design-reference/`) only specifies a flat, single-level sidebar. Two follow-on consequences nobody's written down yet:

- The design-sync `audit` tool (plan #3, §0.3) has no concept of this UI — it can check hover/transition fidelity against the bundle, not a layout the bundle never had.
- Plan #3 §3.6 already flags five *admin* screens (Категории, Акции, Медиатека, Пользователи, Настройки) the mockup never designed and says to "match the neighboring screens" — hierarchical categories on the **storefront sidebar** is a sixth instance of the same problem, on the public site this time, and isn't mentioned there.

---

## Open items — aggregated

Everything above that isn't struck through, in one place:

1. **Cutover to `2.0` (master plan Phase 3–4)** — blocked behind essentially all of plan #3.
2. **Retire `/cms` from the proxy (docker-admin Step 8)** — waiting on "a week of owner usage" *and* now overlaps plan #3's own Stage 4 decision on the same route (see conflict below).
3. **Next.js migration, Stages 3–4** — Stages 0–2 are done (2026-08-20); Stage 3 (admin port) started the same day and has since landed auth + shell + page groups 1–5 of 6 (`login`/`first-register`, `index`, `orders` list + detail, `calendar`/`stock`/`clients`/`analytics`, `categories`/`promotions`/`products`) across four commits — only `media`/`users`/`settings` remains; Stage 4 (cleanup) hasn't started and can't until Stage 3 is done.
4. ~~**Next.js migration 0.1/0.2**~~ — scope the lint/type-check ignore flags, upgrade to Next 16.3 — done 2026-08-20.
5. **~28 `: any` usages** in `apps/cms/src/lib/moysklad` and `apps/cms/src/endpoints` — tracked since 2026-08-14 as a follow-up, currently shielded by the blanket `ignoreDuringBuilds`/`ignoreBuildErrors` flags item 4 above is supposed to narrow.
6. **Design-token wiring** — `tokens.css`/`interactions.css` exist but aren't imported into either app yet; the 86-of-89-transitions-wrong-easing and missing-keyframe gaps `docs/DESIGN-SYNC.md` measured are still live in the shipped Astro code today.
7. ~~**Hierarchical categories UI**~~ — undocumented in every plan and in the design reference; needs an explicit design decision (indent depth, styling) rather than inheriting one from the bundle. Done 2026-08-20: documented the shipped storefront-sidebar behavior as a hand-authored spec, [`docs/design-reference/hierarchical-categories.md`](design-reference/hierarchical-categories.md), so Stage 2 of the Next.js migration has something to build against. The two admin-side gaps flagged alongside this in plan #3 §3.6 (Категории/Акции/etc. screens with no mockup) are still open — this only covers the public sidebar.
8. **`packages/shared-types` fate** — plan #3 §Stage 4 conditions its removal on whether "the legacy Vite/React app is still used" — that app has no retirement plan of its own except master-plan Phase 4, which is now several stages away.
9. **React 18/19 dedupe hack** (`resolve.dedupe` in `astro.config.mjs`) — plan #3 notes it disappears with Astro but the *cause* (legacy root app pinned to React 18, same pnpm workspace) persists until that app is actually deleted.

Deliberately **not** open items — decided and closed, not gaps: blog/content management, a real client/CRM collection, an off-hours pickup surcharge (all declined by the owner in favor of simpler alternatives, per the 2026-08-13 dev log).

---

## Conflicts

**1. Plan #3 quietly obsoletes most of plan #2's Docker work.**
Plan #2 built two Docker services (`web`, `cms`), a reverse proxy, a compression fix, `serverActions.allowedOrigins`, and a `CMS_INTERNAL_URL`/`PUBLIC_PAYLOAD_URL` split — all specifically to make one Astro process and one Next process share a port. Plan #3's whole premise is that this split shouldn't exist; its Stage 4 deletes the `web` service, the second Dockerfile, and every piece of machinery plan #2 Step 2 added. Framed as a "continuation," but in practice it's a rollback of real, verified infrastructure work from six days ago. **Practical implication:** don't invest further polish in `apps/web`'s Docker/proxy setup (e.g. finishing docker-admin Step 8) — it's scheduled for deletion, not hardening.

**2. Two plans both claim ownership of "when to retire `/cms`", and neither says the other exists.**
Docker-admin Step 8 gates it on a week of owner usage of `/admin`. Next-migration Stage 4 lists "решить судьбу Step 8 из старого плана" (decide the fate of the old plan's Step 8) as one of its own cleanup line items — but that's the *proxy prefix* question, and by Stage 4 `/cms` is a same-origin Next route group anyway, a different mechanism than the "proxy allow-list" plan #2 describes. Whoever picks this up should treat plan #2's Step 8 as superseded by plan #3, not as a parallel task to finish first.

**3. Master-plan scope has silently ~doubled with no updated estimate.**
The original plan priced Phases 0–4 at 5.5–7.5 weeks and said nothing about a custom admin or a mid-project framework consolidation. Both happened anyway (good calls individually, per the dev log's reasoning), but no document currently states a revised total timeline or an updated Phase 3 target date — worth a short note in the master plan or `CLAUDE.md` so "when does this ship" has one answer instead of three plans' worth of implicit ones.

**4. Hierarchical categories vs. the design reference.**
Not a plan-vs-plan conflict, but a code-vs-source-of-truth one: `CLAUDE.md` and plan #3 both treat `docs/design-reference/` as the single source of truth for what the storefront should look like ("верстать по спеке, а не по глазам"). The category tree feature has no spec there to build against, so whoever does the Stage-2 storefront port (plan #3) will have to design this UI from scratch mid-migration, the same class of "no spec exists" problem plan #3 §3.6 already flags for five admin screens — just not yet written down for this one.

---

## Suggested order of attack

1. **Decide the timeline question (Conflict 3) first** — it's cheap (one conversation) and changes how much of the below is worth doing before a cutover date is picked.
2. ~~**If proceeding with the Next.js migration:** do Stage 0.1/0.2 (scope lint flags, Next 16.3) before touching Stage 1~~ — done 2026-08-20.
3. ~~**Write a one-paragraph spec for hierarchical categories**~~ (open item 7) before Stage 2 gets there, so it's not designed from scratch mid-port — done 2026-08-20.
4. ~~**Stage 1 (shell + proxy reversal)**~~ — done 2026-08-20, verified live. ~~**Stage 2 (storefront port, 7 page groups)**~~ — done 2026-08-20, all in the same day: legal pages + shared chrome (Navbar/Footer/data layer/islands), how-it-works/contact, homepage (`index`, the largest template in the plan at 297 lines), catalog (index + `[slug]` + `CategorySidebar`), product (`[id]` + `ProductPurchasePanel`/`QuantitySelector`), promotions (`/promotions/:slug`), and finally checkout (`submitOrder()` → a Server Action on the Local API, the one page group the plan itself flagged as needing the most care since it's the only one with a real mutation).
5. **Stage 3 (admin port) is under way** — started 2026-08-20 with auth (3.1) + shell (3.2) + page group 1 of 6 (`login`/`first-register`), verified live including session continuity across the Next/Astro process boundary; a second commit the same day added the 3.3 endpoint→server-function conversions for reads and the first 3.4 Server Actions (page group 3 of 6, `orders` list/detail — status/notes/positions/delete/submit-to-МойСклад), catching and fixing a stale `WEB_URL`/`cors`/`csrf` config bug that silently 401'd every session-reading Server Action along the way (see the 2026-08-20 dev log entry for the mechanism); a third commit added page group 4 of 6 (`calendar`/`stock`/`clients`/`analytics`, all read-only, no new Server Actions needed); a fourth commit added page group 5 of 6 (`categories`/`promotions`/`products/[id]`, genuine writes this time, verified including a promotion's public `/promotions/:slug` page and a media-doc-plus-file cleanup Payload doesn't do automatically). 5 of 6 page groups now done; only `media`/`users`/`settings` remains, per plan section 3.5.
6. **Stop polishing `apps/web`'s Docker setup** (Conflict 1) — anything beyond what's already shipped is work plan #3 deletes.
7. **Revisit `/cms` retirement (open item 2) only after Stage 3 of plan #3 lands** — at that point it's a Next route-group decision, not a proxy one, and Step 8 as written no longer applies cleanly.
