// Shared TypeScript types between apps/cms (Payload) and apps/web (Astro).
// Populated in Phase 1 alongside the Payload collections (products, categories,
// bookings, promotions, settings) — see /Users/a1234/.claude/plans/rosy-booping-spindle.md
export {}
