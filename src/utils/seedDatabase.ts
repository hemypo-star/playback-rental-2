// import { supabase } from "@/integrations/supabase/client";
// import { addDays } from "date-fns";

// Заглушка для seedDatabase
export const seedDatabase = async () => {
  console.log("Seeding disabled — функция ничего не делает.");
};
